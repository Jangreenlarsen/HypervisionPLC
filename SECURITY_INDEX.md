# SECURITY Index - Kendte sikkerhedsproblemer

**Formål:** Kompakt tracking af sikkerhedsanalysen udført 2026-09-02 (fuld kodebase-review i to faser: kortlægning + kildekode-verificering). Ligesom [`BUGS_INDEX.md`](BUGS_INDEX.md) skal denne fil tjekkes **før enhver kodeændring** der rører netværk/API/auth/CLI/Modbus-protokol/ST Logic — både for at undgå at genindføre et fixet problem, og for at opdage om en ændring påvirker et stadig-åbent punkt.

## Hvordan Claude skal bruge denne fil

1. **Før kodeændringer i berørte lag** (web/API, CLI/telnet, Modbus RTU/TCP-parsing, ST Logic VM/compiler, NVS/persistence): skim tabellen (~10 sekunder)
2. Tjek om dit område har åbne (❌ OPEN) eller fixede (✅ FIXED) punkter der er relevante
3. Fixede punkter: **genindfør ikke samme fejl** — kommentaren i koden (søg efter "SECURITY FIX") forklarer hvorfor
4. Åbne punkter: hvis din ændring rører samme kode, overvej at fixe det som del af opgaven, eller flag det til brugeren
5. Ved nyt fix: opdater status her OG i [`BUGS_INDEX.md`](BUGS_INDEX.md) (dette projekts fixes logges begge steder — BUGS_INDEX for kode-historik, denne fil for sikkerheds-overblik)
6. Ved nyt sikkerhedsfund (eget eller brugerens): tilføj en ny række i samme format

## Status Legend

| Symbol | Meaning |
|--------|---------|
| ✅ FIXED | Verificeret rettet i koden (se BUG-ID for detaljer) |
| ❌ OPEN | Identificeret, ikke rettet endnu |
| 📝 DESIGN-BESLUTNING | Ikke et fund/sårbarhed — en bevidst afvejning truffet ved ny funktionalitet, dokumenteret her for gennemsigtighed |
| 🔴 KRITISK | Memory corruption, credential-lækage, eller fuld adgangskontrol-bypass |
| 🟡 HØJ | Betydelig impact, bør rettes snart |
| 🟠 MEDIUM | Mærkbar impact, godt at rette |

## Fund

| # | Titel | Status | Alvor | BUG-ID | Beskrivelse |
|---|-------|--------|-------|--------|-------------|
| 1 | `/api/system/backup` lækker credentials til read-only brugere | ✅ FIXED | 🔴 KRITISK | BUG-328 | Endpointet returnerede WiFi/telnet/HTTP/RBAC-passwords i klartekst til enhver autentificeret bruger (kun `CHECK_AUTH`, ingen rollekontrol). FIX: kræver nu skriverettighed (`CHECK_AUTH_WRITE`) — api_handlers.cpp:4058 |
| 2 | OTA firmware-upload/rollback kræver kun login, ikke skriverettighed | ✅ FIXED | 🔴 KRITISK | BUG-355 | `CHECK_AUTH_OTA` brugte `http_server_check_auth()` (blot gyldig session), i modsætning til alle andre write-endpoints som bruger `CHECK_AUTH_WRITE`+RBAC. Read-only bruger kunne flashe vilkårlig firmware. FIX: mirror'er nu `CHECK_AUTH_WRITE` præcist (uid + `rbac_has_write()`) — ota_handler.cpp:44-62. **Stadig ingen kryptografisk firmware-signaturverifikation** — separat, større emne, ikke løst her |
| 3 | HTTP-auth slået fra som fabriksdefault + svage default-credentials | ✅ FIXED | 🔴 KRITISK | BUG-328 | `http.auth_enabled = 0` var fabriksdefault — nulstillet enhed havde fuldt åbent REST API. FIX: default ændret til 1 — network_config.cpp:54. (Default-passwords `admin/modbus123` og `admin/telnet123` er stadig svage — bør ændres af bruger ved opsætning, ikke selvstændigt rettet) |
| 4 | Global buffer overflow via `MB_WRITE_HOLDINGS`/`MB_READ_HOLDINGS` (INT count uklampet) | ✅ FIXED | 🔴 KRITISK | BUG-324 | Count-arg blev kun clampet for DINT/DWORD (og ufuldstændigt selv der). Et almindeligt ST-script kunne overskrive op til 32 bytes ud over det 16-element `g_mb_multi_reg_buf`. FIX: begge bounds (1-16) håndhævet for alle typer — st_vm.cpp:1273-1284 |
| 5 | ST-parserens rekursionsdybde-guard dækkede ikke parenteser/statement-nesting | ✅ FIXED | 🔴 KRITISK | BUG-325 | BUG-157's oprindelige fix dækkede kun unær-operator-kæder. Dybt nestede parenteser eller IF/FOR/WHILE/CASE-bodies gav ubegrænset C-stack-rekursion → stack overflow ved upload af ST-program. FIX: guard udbredt til `parser_parse_expression()`, `st_parser_parse_statements()`, `st_parser_parse_statements_for_case()` — st_parser.cpp |
| 6 | `LOAD()` (ST Logic) og CLI `load registers` stack-allokerede ~30KB struct | ✅ FIXED | 🔴 KRITISK | BUG-326 | `PersistConfig temp_config;` som stack-lokal — samme struct som `config_save.cpp` bevidst heap-allokerer ("too large for ESP32 stack"). Ethvert script med `LOAD(0);` gav deterministisk stack overflow/reboot. FIX: heap-allokeret (malloc/free) — st_builtin_persist.cpp:104, cli_commands.cpp:1406+1437 |
| 7 | Ingen rate-limiting på fejlede REST API login-forsøg | ✅ FIXED | 🟡 HØJ | BUG-355 | `CHECK_AUTH`/`CHECK_AUTH_WRITE`/`CHECK_AUTH_ROLE` (api_handlers.cpp) og `CHECK_AUTH_OTA` (ota_handler.cpp) tjekkede auth FØR rate-limit — et forkert login returnerede 401 før rate-limit-tjek nås. FIX: rate-limit-tjek flyttet til FØR auth-beslutningen i alle fire makroer |
| 8 | Telnet brute-force lockout omgået ved disconnect/reconnect | ✅ FIXED | 🟡 HØJ | BUG-327 | `auth_attempts`/`auth_lockout_time` blev nulstillet ubetinget ved hver (re)connect — lockout omgået ved at afbryde/genoprette forbindelsen. FIX: lockout-state overlever nu reconnect, nulstilles kun ved naturlig udløb eller succesfuldt login — telnet_server.cpp |
| 9 | Telnet CLI kan DoS'es permanent af én uautentificeret forbindelse | ✅ FIXED | 🟡 HØJ | BUG-355 | `TELNET_READ_TIMEOUT_MS=0` (deaktiveret) + kun ét klient-slot. En åben, aldrig-autentificeret TCP-socket til port 23 blokerede al legitim admin-adgang permanent. FIX: sat til 30 min (samme filosofi som BUG-353's glidende session-token-timeout — aktivitet nulstiller den, kun en reelt forladt forbindelse rammer den) — constants.h:430. **Bevidst IKKE** en kortere pre-auth-specifik timeout (ville kræve auth-state i det lavniveau `TcpClient`, større indgreb i Telnet — den etablerede fallback-adgangsvej) |
| 10 | Stack buffer overflow i `set coil DYNAMIC`/`set holding-reg DYNAMIC` | ✅ FIXED | 🟡 HØJ | BUG-355 | `strncpy(source_copy[32], source_str, colon-source_str)` uden loft mod `sizeof(source_copy)`. FIX: længden clampes nu til `sizeof(source_copy)-1` før strncpy — cli_config_coils.cpp, cli_config_regs.cpp |
| 11 | ST-scripts har uindskrænket adgang til alle Modbus-slaver på bussen | ❌ OPEN (dokumenteret bevidst fravalg) | 🟡 HØJ | — | `validate_slave_addr()` tillader enhver `slave_id` 1-247 — ingen allowlist. Vurderet under BUG-355: en angriber med skriverettighed til at uploade ST-programmer har allerede tilsvarende bus-adgang via `mb write` i CLI'en, så en allowlist ville ikke reelt begrænse den trussel — se `docs/manual/06_Modbus_Interface.md` §6.2 for det dokumenterede ræsonnement. st_builtin_modbus.cpp:47 |
| 12 | `/api/alarms` mangler autentificering helt | ✅ FIXED | 🟠 MEDIUM | BUG-355 | Kun `CHECK_API_ENABLED` + rate-limit, intet `CHECK_AUTH`-kald. Lækkede source-IP og brugernavne fra fejlede forsøg til uautentificerede klienter. FIX: `CHECK_AUTH(req)` tilføjet — api_handlers.cpp |
| 13 | SSE-server understøtter aldrig TLS, selv når HTTPS er aktiveret på API'et | ❌ OPEN (bevidst udskudt) | 🟠 MEDIUM | — | Real-time tællere/registre/GPIO-data (og et engangs-token i URL'en) går altid i klartekst. sse_events.cpp er en håndrullet raw-TCP-socket-server, IKKE bygget på ESP-IDFs `esp_http_server`/`httpd_ssl` som hoved-API'et — TLS-terminering kræver manuel mbedtls_ssl-integration i en allerede kompleks, langtlevende per-klient-task-løkke. Vurderet under BUG-355 som et selvstændigt, større arbejde — udskudt til en dedikeret, separat omgang fremfor at presses ind sammen med 7 andre samtidige sikkerhedsrettelser. sse_events.cpp:1159 |
| 14 | Ingen adgangskontrol på Modbus function code/register-niveau | ❌ OPEN (dokumenteret bevidst fravalg) | 🟠 MEDIUM | — | Enhver der taler på bussen (og kender/gætter slave-ID, eller bruger broadcast) kan skrive til kontrol-registre (`ST_LOGIC_CONTROL_REG_BASE` m.fl.) via standard FC06/FC10. RTU-protokollens fysiske lag har i sagens natur ingen adgangskontrol — dokumenteret som bevidst fravalg i `docs/manual/06_Modbus_Interface.md` §6.2 under BUG-355, ikke en overset mangel. modbus_server.cpp |
| 15 | Cleartext credential-opbevaring i NVS | 🟠 DELVIST FIXED | 🟠 MEDIUM | BUG-352 | `config_save.cpp` beskytter kun med CRC16-integritet, ingen kryptering af den gemte `PersistConfig`-blob. **HTTP/RBAC-passwords er siden BUG-352 SHA-256+salt-hashet, ikke længere klartekst.** WiFi-password er stadig klartekst (skal være det — kræves af WPA2-håndtrykket mod radioen) og Telnet-password er stadig klartekst (separat, endnu ikke omfattet credential-system) |
| 16 | Off-by-one overflow i `set persist group ... add` | ✅ FIXED | 🟠 MEDIUM | BUG-355 | `strcat(range_spec, ",")` var ubounded (i modsætning til det efterfølgende `strncat`). FIX: samme bounded `strncat`-mønster brugt for begge — cli_commands.cpp |
| 17 | ST Logic Modbus Master builtins ignorerede "Modbus Master enabled"-flag | ✅ FIXED | 🟡 HØJ | BUG-329 | Ikke fra den oprindelige analyse, men fundet samme session: `validate_slave_addr()` tjekkede kun om async-systemet var initialiseret, ikke om Master faktisk var slået til — ST-scripts kunne queue MB_*-requests og generere phantom-statistik selv når dashboard viste Master som deaktiveret. FIX: tjekker nu `g_modbus_master_config.enabled` — st_builtin_modbus.cpp:46-58 |
| 18 | Nye REST-endpoints til RBAC-brugerstyring (`GET/POST /api/rbac`, `POST /api/rbac/users`, `DELETE /api/rbac/users/{navn}`) — bevidst parity-beslutning, ikke et fund | 📝 DESIGN-BESLUTNING | — | FEAT-166 | Web-GUI'en fik en "Brugerstyring"-sektion (system.html) som skulle kunne oprette/redigere/slette RBAC-brugere og til/fra RBAC — funktionalitet der hidtil KUN fandtes via CLI (`set user`, `delete user`, `set rbac enable/disable`). De nye endpoints er gated bag `CHECK_AUTH_WRITE` (kun skriverettighed, ikke specifikt "all"-rollen) — **bevidst identisk grænse med CLI'ens EGEN eksisterende model**: `rbac_cli_allowed()` (rbac.cpp) lader allerede enhver bruger med CLI-rolle + skriverettighed køre `set user X roles all privilege read/write` og dermed oprette/eskalere en admin-konto. At kræve noget strengere via REST (fx en dedikeret "all"-rolle-check) ville have været en NY, inkonsistent begrænsning der ikke findes i CLI-vejen — ikke et reelt sikkerhedsløft, bare et andet sted at ramme samme allerede-eksisterende grænse. `GET /api/rbac` (brugerliste) inkluderer ALDRIG password/hash/salt — kun username/roller/privilegium (samme praksis som BUG-352 etablerede for backup-JSON'en, som hex-koder hash+salt i stedet for at sende plaintext). Ingen selv-lockout-beskyttelse (kan slette/nedgradere egen konto) — CLI har heller ingen, samme parity-argument. api_handlers.cpp (RBAC USER MANAGEMENT ENDPOINTS-sektionen) |
| 19 | GitHub-baseret OTA-opdatering (`GET /api/system/ota/github-check`, `POST /api/system/ota/github-install`) — enhedens FØRSTE nogensinde udgående HTTP/HTTPS-klient-forbindelse. Bevidst designafvejning, ikke et fund | 📝 DESIGN-BESLUTNING | — | FEAT-169 | Enheden har hidtil KUN optrådt som server (web/API) + Modbus RTU/TCP master/slave — aldrig som HTTP/HTTPS-klient mod internettet. `/system`-siden fik en "Opdater fra GitHub"-knap der henter release-metadata fra `api.github.com` og evt. `.bin`-asset fra `objects.githubusercontent.com`. **Bevidst KUN manuelt trigget** — ingen baggrunds-polling, enheden taler aldrig ud af sig selv uopfordret (relevant da enheden kan være en LAN-only/offline industriel controller). **TLS-tillid**: rigtige rod-CA'er bundlet (`certs/github_ca_bundle.pem`, embeddet ligesom `certs/servercert.pem` allerede gør for HTTPS-serveren — USERTrust ECC CA + ISRG Root X1, begge selv-signerede rod-ankre, verificeret live 2026-09-05), IKKE `setInsecure()`. Et MITM på netop firmware-download-kanalen er et væsentligt større problem end på et almindeligt API-kald, og #2 (denne tabel) dokumenterer allerede "ingen kryptografisk firmware-signaturverifikation" som en accepteret risiko — at OGSÅ droppe TLS-verifikation på download-kanalen ville lægge endnu et lag oven på samme risiko. **Kendt, accepteret afvejning**: pinner man et konkret rod-CA og GitHub/Let's Encrypt/Sectigo en dag roterer netop dette rod-anker (usandsynligt på kort sigt for disse specifikke, mangeårige rødder — men ikke udelukket), holder GitHub-OTA op med at virke indtil en ny firmware med et opdateret bundle installeres MANUELT — almindelig `.bin`-upload forbliver altid en fungerende fallback-vej, uanset dette. Repoet er offentligt (intet gemt personal access token). Se `docs/RELEASE_PROCEDURE.md` for hvordan releases publiceres (src/ota_handler.cpp, certs/github_ca_bundle.pem) |

## Åbne punkter — prioriteret

Efter BUG-355 (2026-09-xx) er #2, #7, #9, #10, #12, #16 fixet og #14 dokumenteret som bevidst fravalg. Resterende:
1. **#13 SSE TLS** — bevidst udskudt, kræver en dedikeret omgang (manuel mbedtls_ssl-integration i sse_events.cpp's raw-TCP-server)
2. **#11 ST Modbus-scope** — dokumenteret bevidst fravalg (§6.2), men en let, valgfri allowlist (`uint8_t st_modbus_allowlist[16]` tail-felt i PersistConfig, tomt = uændret adfærd) kunne stadig overvejes hvis truslsmodellen ændrer sig (fx flere/mindre betroede ST-forfattere)
3. **#15 cleartext credentials** — delvist fixet (BUG-352, HTTP/RBAC hashet); WiFi/Telnet forbliver klartekst af de dokumenterede grunde
4. **#3** — default-passwords (`admin/modbus123`, `admin/telnet123`) er stadig svage fabriksdefaults; bruger bør ændre dem ved opsætning (se manual §10.3), ikke noget der kan "fixes" i kode

## Relateret

- Fuld analyse (kortlægning + verificering) blev udført 2026-09-02 og gennemgået med brugeren i chat — denne fil er ekstraktet af den samtale.
- Se [`BUGS_INDEX.md`](BUGS_INDEX.md) for BUG-324 til BUG-329 (de 6 fixede punkter, med fil:linje-detaljer og build-reference).
